m, n = map(int, input().split()) # split() separa texto 

""" map() aplica una función a cada elemento de un iterable ((map(funcion, iterable)))
 funcion → lo que quieres hacer (ej: int, str, abs)
iterable → lista, split, etc."""

print((m * n) // 2)



